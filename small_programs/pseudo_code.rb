# 1) A method that returns the sum of two integers
    add two numbers and assign it to the sum.
    
    START
    
    sum = num1 + num2
    
    print sum
    
    END

# 2) a method that takes an array of strings, and returns a string that is all those strings concatenated together
    add two strings together
    return stirngs
    
    START
    
    SET text = string1 + string2
    
    text
    
    END
    
# 3) a method that takes an array of integers, and returns a new array with every other element from the original array, starting with the first element.
    array of integers
    iterate through the array of integers
      set a new array
      if the iterator is even
        save integer to the new array
      otherwise
        next
      print new array
      
    START
    
    #Array of integers = numbers
    
    SET iterator = 0
    SET other_numbers = []
    
    WHILE iterator <= length of numbers
      if iterator.even
        other_numbers << the first number of the collection "numbers"
      else
        mext
      
    iterator += 1
    
    PRINT other_numbers
    
    END
    
    
    
    
    
    